# GENERATED by journalfig._stylegen from SPECS -- do not edit.
# Change the JournalSpec in _specs.py and run `python -m journalfig._stylegen`.
# Shared settings live in _base.mplstyle, which is hand-written and applied first.
#
# PLOS figure style
# Spec: https://journals.plos.org/plosone/s/figures
#   PLOS, Figures, retrieved 2026-08-24
#   widths: double 190.5 mm, single 66.8 mm
#   text 8-12 pt; lines >= 0.57 pt; raster >= 300 dpi
#
# PLOS states a resolution ceiling as well as a floor -- 300 to 600 dpi, and no more.
# JournalSpec models floors only, so the upper bound is not enforced.

# Single column at the inverse golden ratio, the same default journalfig.figsize() applies, so a
# bare plt.subplots() here and jf.subplots(..., width="single") agree.
figure.figsize:                 2.629921, 1.625381
figure.titlesize:               8

font.family:                    sans-serif
font.sans-serif:                Arial, Helvetica, Helvetica Neue, Nimbus Sans, Liberation Sans, DejaVu Sans
font.size:                      8

mathtext.fontset:               custom
mathtext.rm:                    Arial
mathtext.it:                    Arial:italic
mathtext.bf:                    Arial:bold
mathtext.sf:                    Arial
mathtext.tt:                    Courier New

axes.linewidth:                 0.6
axes.labelsize:                 8
axes.titlesize:                 8
axes.titleweight:               bold
axes.titlelocation:             left

xtick.labelsize:                8
xtick.major.size:               3
xtick.major.width:              0.6
xtick.major.pad:                2.5
xtick.minor.size:               1.8
xtick.minor.width:              0.5

ytick.labelsize:                8
ytick.major.size:               3
ytick.major.width:              0.6
ytick.major.pad:                2.5
ytick.minor.size:               1.8
ytick.minor.width:              0.5

# markersize 4 pt = 1.41 mm printed.
lines.markersize:               4
lines.markeredgewidth:          0.7

patch.linewidth:                0.6
hatch.linewidth:                0.6
grid.linewidth:                 0.5
errorbar.capsize:               2

legend.fontsize:                8
legend.title_fontsize:          8
legend.handlelength:            1.8
