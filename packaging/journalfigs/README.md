# journalfigs

An alias for [journalfig](https://github.com/Atilaac/journalfig). Installing it installs the real
package and makes `import journalfigs` return the `journalfig` module, so the plural spelling works
and cannot be claimed by anyone else.

Use `pip install journalfig` directly; this exists only to catch the other spelling.
